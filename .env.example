# ====================================
# V&A WATERFRONT COMPLIANCE SYSTEM
# Environment Variables Configuration
# ====================================

# ----------------------
# DATABASE (REQUIRED)
# ----------------------
# PostgreSQL connection string
# Local: postgresql://user:password@localhost:5432/compliance_engine
# Vercel Postgres: Auto-populated when you connect database
# Neon/Supabase: Copy from their dashboard
DATABASE_URL="postgresql://user:password@localhost:5432/compliance_engine"

# Optional: Direct URL for migrations (same as DATABASE_URL for most cases)
DIRECT_URL="postgresql://user:password@localhost:5432/compliance_engine"

# ----------------------
# AUTHENTICATION (REQUIRED FOR PRODUCTION)
# ----------------------
# Generate with: openssl rand -base64 32
# NextAuth v5 uses AUTH_SECRET (not NEXTAUTH_SECRET)
AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Your deployment URL (required in production)
# Development: Auto-detected as http://localhost:3000
# Production: https://your-app.vercel.app or your custom domain
NEXTAUTH_URL="http://localhost:3000"

# ----------------------
# FEATURE FLAGS (OPTIONAL)
# ----------------------
# Enable demo mode (shows helpful UI hints)
NEXT_PUBLIC_DEMO_MODE="true"

# Enable mock mode (bypasses database, uses mock data)
# Set to "false" for production with real database
NEXT_PUBLIC_MOCK_MODE="false"

# ----------------------
# DEPLOYMENT NOTES
# ----------------------
# For Vercel:
#   1. DATABASE_URL - Set in project settings or use Vercel Postgres
#   2. AUTH_SECRET - Generate new secret
#   3. NEXTAUTH_URL - Set to your vercel.app URL
#   4. After deploy, run: npx prisma migrate deploy && npm run db:seed
#
# For Railway/Render:
#   - PostgreSQL is auto-provisioned
#   - DATABASE_URL auto-populated
#   - Set AUTH_SECRET and NEXTAUTH_URL manually
# ----------------------

# Database Configuration
# For local Postgres, DIRECT_URL can equal DATABASE_URL
# For Nile or pooled connections, use separate URLs
DATABASE_URL="postgresql://user:password@localhost:5432/compliance_engine"
DIRECT_URL="postgresql://user:password@localhost:5432/compliance_engine"

# Authentication (use NEXTAUTH_SECRET, not AUTH_SECRET)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Optional: NextAuth URL (auto-detected in development)
# NEXTAUTH_URL="http://localhost:3000"

# Feature Flags
DEMO_MODE=true
MOCK_MODE=true

# Node Environment
NODE_ENV=development
